# Copia a ".env" en la raiz del repo (docker compose lo lee solo).
# Si el 8000 esta ocupado, usa 8001 y la misma URL en VITE_API_BASE_URL.

BACKEND_HOST_PORT=8001
FRONTEND_HOST_PORT=8081
DB_HOST_PORT=3306
VITE_API_BASE_URL=http://localhost:8001
